# 检查 GitHub 部署状态指南

## 📋 当前 Git 状态

- **仓库地址**: `https://github.com/Satorrriko/shiawasenoito.git`
- **当前分支**: `main`
- **状态**: 与 `origin/main` 同步

## 🔍 如何检查部署状态

### 方法 1: 检查 GitHub Actions（推荐）

1. **访问 GitHub 仓库**
   ```
   https://github.com/Satorrriko/shiawasenoito
   ```

2. **查看 Actions 标签页**
   - 点击仓库顶部的 **Actions** 标签
   - 查看 "Deploy to GitHub Pages" 工作流
   - 检查最新的运行状态：
     - ✅ **绿色对勾** = 部署成功
     - ❌ **红色叉** = 部署失败
     - 🟡 **黄色圆点** = 正在运行

3. **查看部署日志**
   - 点击最新的工作流运行
   - 检查各个步骤：
     - ✅ Checkout
     - ✅ Setup Node.js
     - ✅ Install dependencies
     - ✅ Build
     - ✅ Setup Pages
     - ✅ Upload artifact
     - ✅ Deploy to GitHub Pages

### 方法 2: 检查 GitHub Pages 设置

1. **进入仓库设置**
   - 点击仓库的 **Settings**
   - 左侧菜单选择 **Pages**

2. **检查配置**
   - **Source**: 应该是 **"GitHub Actions"**（不是 "Deploy from a branch"）
   - **Custom domain**: 如果有自定义域名会显示
   - **部署状态**: 会显示最后一次部署的时间

3. **查看部署的 URL**
   - 如果已部署，会显示：
     ```
     https://satorrriko.github.io/shiawasenoito/
     ```

### 方法 3: 直接访问网站

访问以下 URL 检查是否可访问：

```
https://satorrriko.github.io/shiawasenoito/
```

- ✅ **能访问** = 已部署成功
- ❌ **404 错误** = 未部署或部署失败
- ❌ **显示源代码** = 部署了源代码而不是构建产物

### 方法 4: 检查浏览器开发者工具

1. 访问 `https://satorrriko.github.io/shiawasenoito/`
2. 打开开发者工具（F12）
3. 查看 **Network** 标签页
4. 检查资源加载：
   - ✅ 应该从 `/shiawasenoito/assets/...` 加载
   - ❌ 如果从 `/src/...` 加载 = 部署了源代码

## 🚨 常见问题

### 问题 1: Actions 中没有工作流

**原因**: `.github/workflows/deploy.yml` 文件未提交到 Git

**解决**:
```bash
git add .github/workflows/deploy.yml
git commit -m "Add GitHub Pages deployment workflow"
git push origin main
```

### 问题 2: 工作流运行失败

**检查**:
1. 进入 Actions 标签页
2. 点击失败的工作流
3. 查看错误信息
4. 常见错误：
   - `npm ci` 失败 → 需要更新 `package-lock.json`
   - 路径错误 → 检查工作流中的路径配置
   - 权限错误 → 检查 Pages 设置

### 问题 3: 部署成功但网站无法访问

**检查**:
1. 确认访问的 URL 正确（包含仓库名）
2. 清除浏览器缓存
3. 检查 Pages 设置中的 Source 配置

### 问题 4: 显示源代码而不是构建后的网站

**原因**: GitHub Pages Source 设置为 "Deploy from a branch"

**解决**:
1. Settings → Pages
2. Source 改为 "GitHub Actions"
3. 保存后重新触发部署

## 📝 快速检查清单

- [ ] 访问 `https://github.com/Satorrriko/shiawasenoito/actions`
- [ ] 查看最新的 "Deploy to GitHub Pages" 工作流状态
- [ ] 检查 Settings → Pages 配置
- [ ] 访问 `https://satorrriko.github.io/shiawasenoito/`
- [ ] 检查浏览器控制台是否有错误
- [ ] 验证资源路径是否正确

## 🔧 如果需要重新部署

### 方法 1: 推送代码触发自动部署

```bash
git add .
git commit -m "Update deployment"
git push origin main
```

### 方法 2: 手动触发工作流

1. 进入 Actions 标签页
2. 选择 "Deploy to GitHub Pages" 工作流
3. 点击 "Run workflow" 按钮
4. 选择分支（main）
5. 点击 "Run workflow"

## 📊 部署状态检查命令

在本地可以运行：

```bash
# 检查工作流文件是否存在
git ls-files | grep ".github/workflows"

# 检查最近的提交
git log --oneline -5

# 检查远程仓库
git remote -v
```

## 🎯 预期结果

如果部署成功，你应该能够：

1. ✅ 在 Actions 中看到成功的部署工作流
2. ✅ 访问 `https://satorrriko.github.io/shiawasenoito/` 看到网站
3. ✅ 浏览器控制台没有 404 错误
4. ✅ 资源从 `/shiawasenoito/assets/...` 加载

